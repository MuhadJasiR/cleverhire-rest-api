import 'package:flutter/material.dart';

class ChatScreenSeeker extends StatelessWidget {
  const ChatScreenSeeker({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Chat box"),
        centerTitle: true,
      ),
    );
  }
}
